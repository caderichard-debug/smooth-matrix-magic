services:
  - type: web
    name: smooth-matrix-magic
    runtime: static
    buildCommand: npm ci && npm run build
    staticPublishPath: dist/client
    pullRequestPreviewsEnabled: true
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
